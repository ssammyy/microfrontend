import {Confirmed

.
Validator
}
from
'./confirmed.validator';

describe('Confirmed.Validator', () => {
  it('should create an instance', () => {
    expect(new Confirmed.Validator()).toBeTruthy();
  });
});
