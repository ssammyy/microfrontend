import {SignupFormPipePipe} from './signup-form-pipe.pipe';

describe('SignupFormPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SignupFormPipePipe();
    expect(pipe).toBeTruthy();
  });
});
